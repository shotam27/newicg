services:
  - type: web
    name: icg-game
    runtime: node
    plan: free
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
